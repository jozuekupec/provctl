# ============================================================
# GENERATED BY PROVCTL — DO NOT EDIT
# default catch-all virtual host
# ============================================================
<VirtualHost *:80>
    ServerName provctl-default.invalid
    <Location />
        Require all denied
    </Location>
</VirtualHost>

<VirtualHost *:443>
    ServerName provctl-default.invalid
    SSLEngine on
    SSLCertificateFile {{ .CertificateFile }}
    SSLCertificateKeyFile {{ .KeyFile }}
    <Location />
        Require all denied
    </Location>
</VirtualHost>
